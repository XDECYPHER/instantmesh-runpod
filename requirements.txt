# Файл для справки/локального теста — сама сборка образа не использует
# requirements.txt, зависимости ставятся напрямую в Dockerfile.
runpod
requests
torch==2.1.2
torchvision==0.16.2
# Остальные зависимости SF3D ставятся из requirements.txt репозитория
# Stability-AI/stable-fast-3d прямо внутри Dockerfile.
